{
  "name": "twilio-poc",
  "version": "1.0.0",
  "description": "POC for SMS service using Twilio",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "clean": "rm -rf dist",
    "build": "tsc --project ./",
    "prestart": "npm run build",
    "start": "node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jai Prakash",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "twilio": "^3.50.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.8",
    "@types/node": "^14.14.6",
    "ts-node": "^9.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.0.5"
  }
}
